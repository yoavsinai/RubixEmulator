[package]
name = "RubixEmulator"
version = "0.1.0"
edition = "2024"

[lib]
name = "rubixemulator"
path = "src/lib.rs"

[dependencies]
crossterm = "0.27"
three-d = { version = "0.18", features = ["egui-gui"] }
